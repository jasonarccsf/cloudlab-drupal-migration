---
# See: https://github.com/geerlingguy/drupal-vm/blob/master/docs/deployment/multisite.md
# See: https://github.com/geerlingguy/ansible-role-drupal/blob/master/molecule/default/test-vars.yml

#
# PHP 8.0
#
php_version: "8.0"
# php_enable_webserver: false
# php_enable_php_fpm: true
# php_fpm_listen: "127.0.0.1:9000"

#
# Drupal 7
#

drupal_deploy_version: "master"
# drupal_deploy_version: "7.98"

drupal_deploy_dir: "/var/www/drupal"
drupal_core_path: "{{ drupal_deploy_dir }}/web"

drupal_domain: "local.my-drupal-site.com"

apache_listen_ip: "*"
apache_listen_port: 80
apache_listen_port_ssl: 443

apache_create_vhosts: true
apache_vhosts_filename: "vhosts.conf"

# apache_remove_default_vhost: true

apache_global_vhost_settings: |
  DirectoryIndex index.php index.html

# NOTE: apache_vhosts sites are created in /etc/apache2/sites-available/vhosts.conf.
apache_vhosts:
  # Drupal VM's default domain, evaluating to whatever `vagrant_hostname` is set to (drupalvm.test by default).
  - servername: "drupal-7-sfdpa.site"
    serveralias: "www.{{ drupal_domain }}"
    documentroot: "{{ drupal_core_path }}"

apache_mods_enabled:
  - rewrite
apache_mods_disabled: []

# mysql_python_package_debian: python3-mysqldb

# mysql_databases:
#   - name: drupal
#     encoding: utf8
#     collation: utf8_general_ci

#   # - name: drupal_two
#   #   encoding: utf8
#   #   collation: utf8_general_ci

# mysql_users:
#   - name: drupal
#     host: "%"
#     password: drupal
#     priv: "drupal.*:ALL"

#   # - name: drupal-two
#   #   host: "%"
#   #   password: drupal-two
#   #   priv: "drupal_two.*:ALL"
